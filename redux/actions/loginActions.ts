import templateAction from "../../constants/templateAction";

export const INPUT_EMAIL_USERNAME = "INPUT_EMAIL_USERNAME";
export const INPUT_PASSWORD = "INPUT_PASSWORD";

export const inputEmailUsername = templateAction(INPUT_EMAIL_USERNAME);
export const inputPassword = templateAction(INPUT_PASSWORD);
