const ColoredLine = ({ color } : any) => (
    <div
        style={{
            width: '100%',
            backgroundColor: color,
            height: '1px'
        }}
    ></div>
);

export default ColoredLine;