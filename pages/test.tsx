import React from 'react';
import Loading from '../components/Loading';
 
const test = () => {
    return (
        <div>
            <Loading size={50}/>
        </div>

    )
};

export default test;